/// App typography tokens for the Ahara design system.
///
/// No inline TextStyle with literal sizes — always pull from here.
// TODO(F1): Populate with values from docs/designs/F1/ once design is locked.
abstract final class AppTypography {
  // TODO(F1): font_family_headings (Inter / Geist or similar)
  // TODO(F1): font_family_body
  // TODO(F1): font_family_numeric (tabular figures)
  // TODO(F1): scale — display, headline (>=18sp), title, body (>=14sp),
  //           label, numeric_large
  // Accessibility: body >=14sp, headings >=18sp, WCAG AA contrast.
}
